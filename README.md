# KSU Vet School Study Mate
GitHub Pages-ready PWA.

## Upload
Upload all files and the `images` folder to the repository root. Do not upload only the ZIP.

## Enable GitHub Pages
Settings → Pages → Deploy from a branch → `main` → `/ (root)`.

The app uses separate image assets so the HTML files remain small.
